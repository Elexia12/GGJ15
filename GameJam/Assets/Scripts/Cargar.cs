using UnityEngine;
using System.Collections;

public class Cargar : MonoBehaviour {

	// Use this for initialization
	void Start () {
	/*			GameObject[] objetos = Resources.LoadAll<GameObject> ("");
				GameObject tmp = (GameObject)Instantiate (objetos [0]);
				tmp.transform.localPosition = new Vector3 (0.0f, 2.0f, 0.0f);

		tmp = (GameObject)Instantiate (objetos[0]);
		tmp.transform.localPosition = new Vector3 (0.0f, -3.0f, 0.0f);*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
